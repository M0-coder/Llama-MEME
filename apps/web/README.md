# Web application boundary

The public website will live here and will be deployable independently by Vercel while remaining in the same monorepo.

No production web application is introduced in foundation v0.1. The next web implementation must consume the canonical token specification rather than duplicate token values manually.
